import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: ["Neutra Text Alt","Neutra Text Light","Neutra Text TF","Neutra Text"].join(",")
  }
});
